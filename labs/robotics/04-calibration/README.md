# Drill 4 — Calibration sweep

Fit a declared parameter on calibration rows, then report error on untouched held-out rows. Pass only if the selected value and both errors are reproducible. Extend with confidence intervals and a robust loss.
