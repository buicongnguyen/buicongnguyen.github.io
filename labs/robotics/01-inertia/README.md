# Drill 1 — Mass and inertia validator

Implement `validate_inertia` in `starter.py`. It must reject non-finite, asymmetric, non-positive, and triangle-inequality-violating tensors. Run `python -m pytest test_lab.py -q`. Use `solution.py` only after recording the failed assumption.
