# Drill 5 — Reset and determinism regression

Run the same seeded rollout twice, a different seed once, and 100 resets. Compare with declared absolute/relative tolerances, reject non-finite state, and report the first divergent sample—not only a final checksum.
