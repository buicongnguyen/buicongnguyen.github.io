# Drill 6 — Fair benchmark harness

The harness separates warmup from measured iterations and records workload identity. Extend it to synchronize the target backend, report median and percentiles, and serialize hardware/configuration alongside results.
